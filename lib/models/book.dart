class Book{
  String title; // used in book detail
  String author; // used in book detail
  String preview;//thumbnail image
  String link; // link to web viewer
  String category; // used to build main list
  Book(this.title, this.author, this.preview, this.link, this.category);
}